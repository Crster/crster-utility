<Page
    x:Class="App.Pages.NotebookPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:App.Controls">

    <Grid Padding="20,16,20,12">
        <Grid x:Name="NotebookSurface" MaxWidth="920" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="14" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Padding="6" HorizontalAlignment="Left" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
                    <StackPanel Orientation="Horizontal" Spacing="2">
                        <Button Tag="title" Click="ToolButton_Click" Width="34" Height="32" Padding="3" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Title"><TextBlock Text="#" FontSize="18" FontWeight="SemiBold" /></Button>
                        <Button Tag="description" Click="ToolButton_Click" Width="34" Height="32" Padding="3" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Description"><TextBlock Text="##" FontSize="13" /></Button>
                        <Button Tag="important" Click="ToolButton_Click" Width="34" Height="32" Padding="3" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Important"><TextBlock Text="&quot;A&quot;" FontWeight="Bold" /></Button>
                        <Button Tag="mono" Click="ToolButton_Click" Width="34" Height="32" Padding="3" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Monospace"><TextBlock Text="'A'" FontFamily="Cascadia Mono" FontSize="12" /></Button>
                        <Button Tag="italic" Click="ToolButton_Click" Width="34" Height="32" Padding="3" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Italic"><TextBlock Text="(A)" FontStyle="Italic" /></Button>
                        <Border Width="1" Height="20" Margin="5,0" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                        <Button Tag="password" Click="ToolButton_Click" Width="34" Height="32" Padding="5" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Password"><FontIcon Glyph="&#xE72E;" /></Button>
                        <Button Tag="file" Click="ToolButton_Click" Width="34" Height="32" Padding="5" AllowFocusOnInteraction="False" ToolTipService.ToolTip="File"><SymbolIcon Symbol="Attach" /></Button>
                        <Button Tag="image" Click="ToolButton_Click" Width="34" Height="32" Padding="5" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Image"><SymbolIcon Symbol="Pictures" /></Button>
                        <Button Tag="table" Click="ToolButton_Click" Width="34" Height="32" Padding="5" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Table"><FontIcon Glyph="&#xF232;" /></Button>
                        <Button Tag="todo" Click="ToolButton_Click" Width="34" Height="32" Padding="5" AllowFocusOnInteraction="False" ToolTipService.ToolTip="Todo"><SymbolIcon Symbol="Bullets" /></Button>
                    </StackPanel>
                </Border>
                <Button Grid.Column="1" Tag="new" Click="ToolButton_Click" Content="New note" VerticalAlignment="Center" Height="32" Padding="12,0" AllowFocusOnInteraction="False">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE710;" FontSize="12" />
                                <TextBlock Text="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </Grid>

        <ListView x:Name="BlocksHost"
                  Grid.Row="2"
                  Padding="0"
                  HorizontalContentAlignment="Stretch"
                  SelectionMode="None"
                  IsItemClickEnabled="False"
                  DoubleTapped="BlocksHost_DoubleTapped"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollMode="Enabled"
                  ScrollViewer.HorizontalScrollMode="Disabled"
                  Background="Transparent"
                  BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:Noteblock Loaded="Block_Loaded" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Top" />
                    <Setter Property="MinHeight" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        </Grid>
    </Grid>
</Page>
