<Page
    x:Class="App.Pages.NotebookPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:App.Controls">

    <Grid Padding="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="16" /><RowDefinition Height="*" /></Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Spacing="4">
            <ToggleButton x:Name="ParagraphTool" Tag="paragraph" Click="ToolButton_Click" Width="36" Height="36" Padding="6" ToolTipService.ToolTip="Paragraph"><SymbolIcon Symbol="Font" /></ToggleButton>
            <ToggleButton x:Name="ImageTool" Tag="image" Click="ToolButton_Click" Width="36" Height="36" Padding="6" ToolTipService.ToolTip="Image"><SymbolIcon Symbol="Pictures" /></ToggleButton>
            <ToggleButton x:Name="FileTool" Tag="file" Click="ToolButton_Click" Width="36" Height="36" Padding="6" ToolTipService.ToolTip="File"><SymbolIcon Symbol="Attach" /></ToggleButton>
            <ToggleButton x:Name="TableTool" Tag="table" Click="ToolButton_Click" Width="36" Height="36" Padding="6" ToolTipService.ToolTip="Table"><FontIcon Glyph="&#xF232;" /></ToggleButton>
            <ToggleButton x:Name="TodoTool" Tag="todo" Click="ToolButton_Click" Width="36" Height="36" Padding="6" ToolTipService.ToolTip="Todo"><SymbolIcon Symbol="Bullets" /></ToggleButton>
        </StackPanel>

        <ListView x:Name="BlocksHost"
                  Grid.Row="2"
                  Padding="0"
                  HorizontalContentAlignment="Stretch"
                  SelectionMode="None"
                  IsItemClickEnabled="False"
                  DoubleTapped="BlocksHost_DoubleTapped"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollMode="Enabled"
                  ScrollViewer.HorizontalScrollMode="Disabled"
                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  CornerRadius="12">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:Noteblock Loaded="Block_Loaded" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Top" />
                    <Setter Property="MinHeight" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</Page>
